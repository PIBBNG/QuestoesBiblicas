const initialState = {
    question_ids_list: [],
    current_question: {},
}

export function questionReducer(state=initialState, action){
    switch(action.type){
        default:
            return state;
    }
}